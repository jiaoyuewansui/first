! function(e, t) {
    function n() { t.body ? t.body.style.fontSize = 12 * o + "px" : t.addEventListener("DOMContentLoaded", n) } //设置body字体大小，有body就设置，没有就在页面加载完设置
    //设置html字体大小
    function d() {
        var e = i.clientWidth / 10;
        i.style.fontSize = e + "px"
    }
    var i = t.documentElement,
        o = e.devicePixelRatio || 1; //得到物理像素比
    //监听页面大小发生变化
    if (n(), d(), e.addEventListener("resize", d), e.addEventListener("pageshow", function(e) { e.persisted && d() }), o >= 2) {
        //epersisted,判断页面是否缓存，是true就是缓存的也要计算rem的值
        var a = t.createElement("body"),
            s = t.createElement("div");
        //有些浏览器不支持0.5的写法，这样就支持了s
        s.style.border = ".5px solid transparent", a.appendChild(s), i.appendChild(a), 1 === s.offsetHeight && i.classList.add("hairlines"), i.removeChild(a)
    }
}(window, document); //立即函数